<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="csrf-token" content="{{ csrf_token() }}">
        <title>@yield('title', config('app.name', 'EasyColoc'))</title>

        <!-- Tailwind CSS CDN -->
        <script src="https://cdn.tailwindcss.com"></script>
        
        <!-- Alpine.js CDN -->
        <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    </head>
    <body class="bg-gray-50 min-h-screen flex flex-col">
        <!-- Navbar -->
        <nav class="h-14 bg-white border-b border-gray-200 shadow-sm flex items-center px-6">
            <div class="flex-1 flex items-center">
                <!-- Logo -->
                <a href="{{ route('dashboard') }}" class="flex items-center space-x-2 text-blue-600 text-xl font-bold hover:text-blue-700">
                    <span>🏠</span>
                    <span>EasyColoc</span>
                </a>
            </div>
            
            <div class="flex items-center space-x-4">
                @role('admin')
                    <a href="{{ route('admin.index') }}" class="bg-red-100 text-red-700 px-3 py-1 rounded-full text-sm font-semibold hover:bg-red-200">
                        ⚙️ Admin
                    </a>
                @endrole
                
                <span class="text-gray-700">
                    👤 {{ auth()->user()->name }} ★{{ auth()->user()->reputation }}
                </span>
                
                <a href="{{ route('profile.edit') }}" class="text-gray-600 hover:text-gray-900">
                    Profil
                </a>
                
                <form method="POST" action="{{ route('logout') }}" class="inline">
                    @csrf
                    <button type="submit" class="text-gray-600 hover:text-gray-900">Déconnexion</button>
                </form>
            </div>
        </nav>

        <div class="flex flex-1">
            <!-- Sidebar -->
            <aside class="w-60 bg-white border-r border-gray-200 min-h-screen sticky top-0">
                @role('admin')
                    {{-- SIDEBAR ADMIN --}}
                    <div class="p-4">
                        <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">ADMINISTRATION</h3>
                        <div class="space-y-1">
                            <a href="{{ route('admin.index', ['section' => 'statistics']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->get('section') == 'statistics' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                📊 Statistiques
                            </a>
                            <a href="{{ route('admin.index', ['section' => 'users']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->get('section') == 'users' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                👥 Utilisateurs
                            </a>
                            <a href="{{ route('admin.index', ['section' => 'colocations']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->get('section') == 'colocations' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                🏠 Colocations
                            </a>
                            <a href="{{ route('admin.index', ['section' => 'banned']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->get('section') == 'banned' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }} text-red-600">
                                🚫 Bannis
                            </a>
                        </div>
                        
                        <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">MON ESPACE</h3>
                        <div class="space-y-1">
                            <a href="{{ route('dashboard') }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('dashboard') ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                📊 Dashboard
                            <a href="{{ route("wallet.index") }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs("wallet.*") ? "bg-blue-50 text-blue-600 border-l-2 border-blue-600" : "text-gray-600 hover:bg-gray-50" }}">
                                💰 Mon Wallet
                                @php $walletBalance = auth()->user()->wallet?->balance ?? 0; @endphp
                                <span class="ml-auto text-xs font-bold {{ $walletBalance > 0 ? "text-green-600" : "text-gray-400" }}">
                                    {{ number_format($walletBalance, 2) }} €
                                </span>
                            </a>
                            </a>
                            <a href="{{ route('profile.edit', ['section' => 'info']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'info' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                👤 Profil
                            </a>
                            @if(auth()->user()->activeMembership)
                                <a href="{{ route('colocations.show', auth()->user()->activeMembership->colocation_id) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('colocations.show') ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    🏠 Ma Colocation
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'colocations']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'colocations' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    🏠 Mes Colocations
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'expenses']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'expenses' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    💰 Mes Dépenses
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'activity']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'activity' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    📈 Activité
                                </a>
                            @else
                                <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3 mt-6">REJOINDRE</h3>
                                <div class="space-y-1">
                                    <a href="{{ route('colocations.create') }}" class="flex items-center px-3 py-2 text-sm rounded-lg text-green-600 hover:bg-green-50">
                                        ➕ Créer une colocation
                                    </a>
                                </div>
                            @endif
                        </div>
                    </div>
                @else
                    {{-- SIDEBAR USER --}}
                    <div class="p-4">
                        <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">MON ESPACE</h3>
                        <div class="space-y-1">
                            <a href="{{ route('dashboard') }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('dashboard') ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                📊 Dashboard
                            <a href="{{ route("wallet.index") }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs("wallet.*") ? "bg-blue-50 text-blue-600 border-l-2 border-blue-600" : "text-gray-600 hover:bg-gray-50" }}">
                                💰 Mon Wallet
                                @php $walletBalance = auth()->user()->wallet?->balance ?? 0; @endphp
                                <span class="ml-auto text-xs font-bold {{ $walletBalance > 0 ? "text-green-600" : "text-gray-400" }}">
                                    {{ number_format($walletBalance, 2) }} €
                                </span>
                            </a>
                            </a>
                            <a href="{{ route('profile.edit', ['section' => 'info']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'info' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                👤 Profil
                            </a>
                        </div>
                        
                        @if(auth()->user()->activeMembership)
                            <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3 mt-6">MA COLOCATION</h3>
                            <div class="space-y-1">
                                <a href="{{ route('colocations.show', auth()->user()->activeMembership->colocation_id) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('colocations.show') ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    🏠 Voir ma coloc
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'colocations']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'colocations' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    🏠 Mes Colocations
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'expenses']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'expenses' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    💰 Mes Dépenses
                                </a>
                                <a href="{{ route('profile.edit', ['section' => 'activity']) }}" class="flex items-center px-3 py-2 text-sm rounded-lg {{ request()->routeIs('profile.edit') && request()->get('section') == 'activity' ? 'bg-blue-50 text-blue-600 border-l-2 border-blue-600' : 'text-gray-600 hover:bg-gray-50' }}">
                                    📈 Activité
                                </a>
                            </div>
                        @else
                            <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3 mt-6">REJOINDRE</h3>
                            <div class="space-y-1">
                                <a href="{{ route('colocations.create') }}" class="flex items-center px-3 py-2 text-sm rounded-lg text-green-600 hover:bg-green-50">
                                    ➕ Créer une colocation
                                </a>
                            </div>
                        @endif
                    </div>
                @endrole
            </aside>

            <!-- Main Content -->
            <main class="flex-1 p-6">
                <!-- Flash Messages -->
                @if(session('success'))
                    <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-xl mb-4 flex items-center gap-3">
                        <span>✅</span>
                        <span>{{ session('success') }}</span>
                    </div>
                @endif
                
                @if(session('error'))
                    <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-xl mb-4 flex items-center gap-3">
                        <span>❌</span>
                        <span>{{ session('error') }}</span>
                    </div>
                @endif
                
                @if($errors->any())
                    <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-xl mb-4">
                        <ul class="list-disc list-inside">
                            @foreach($errors->all() as $error)
                                <li>{{ $error }}</li>
                            @endforeach
                        </ul>
                    </div>
                @endif

                @yield('content')
            </main>
        </div>

        <!-- Footer -->
        <footer class="bg-white border-t border-gray-200 text-center text-sm text-gray-400 py-4">
            © 2026 EasyColoc
        </footer>
    </body>
</html>
